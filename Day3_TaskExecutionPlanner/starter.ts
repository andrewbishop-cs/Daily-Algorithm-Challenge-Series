/**
 * Day 3 Challenge: Task Execution Planner
 *
 * Warm up problems
 *  Leetcode 207 - Course Schedule
 *  Leetcode 210 - Course Schedule II
 * 
 * You have a list of jobs and dependency pairs [succ, pre], meaning
 * “pre must run before succ.” Return one valid execution order of all jobs,
 * or an empty array if no valid schedule exists (due to cycles).
 *
 * @param jobs          List of all job names
 * @param dependencies  Array of [succ, pre] pairs
 * @returns             A topologically sorted array of job names, or [] on cycle
 */
export function planJobExecution(
    jobs: string[],
    dependencies: Array<[string, string]>
  ): string[] {
    
    return [];
  }
  