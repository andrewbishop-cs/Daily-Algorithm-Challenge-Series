/**
 * Day 8 Challenge: Dynamic Resource Allocation
 *
 * Warm up problems
 *  Leetcode 455 - Assign Cookies
 *  Leetcode 452 - Minimum Number of Arrows to Burst Balloons
 * 
 * Given a list of events (each with a start and end time),
 * select the maximum number of non‑overlapping events that
 * can run on a single resource.
 *
 * Implement `maxNonOverlappingEvents` by sorting events by end time
 * and then greedily picking compatible ones.
 */

export interface Event {
    start: number;
    end: number;
  }
  
  export function maxNonOverlappingEvents(events: Event[]): Event[] {

    return [];
  }
  